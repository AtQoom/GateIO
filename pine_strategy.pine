//@version=5
strategy("SOLUSDT MTF 전략 (자동매매)", overlay=true)
// 전략 조건 및 알림
rsi = ta.rsi(close, 6)
if (rsi < 24)
    strategy.entry("Long", strategy.long)
    alert("long", alert.freq_once_per_bar)
if (rsi > 76)
    strategy.entry("Short", strategy.short)
    alert("short", alert.freq_once_per_bar)